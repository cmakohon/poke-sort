{
  "name": "@poke-sort/desktop",
  "version": "1.0.1",
  "private": true,
  "description": "PokeSort \u2014 Pok\u00e9mon card scanner and physical sorter",
  "author": "dishwasher-detergent",
  "main": "dist/main.cjs",
  "scripts": {
    "build": "tsup",
    "dev": "pnpm -w run build && electron dist/main.cjs",
    "dev:catalog": "POKE_SORT_DATA_DIR=../server/.poke-sort-catalog pnpm dev",
    "bundle:server": "rm -rf .server-bundle && pnpm --filter @poke-sort/server deploy --prod .server-bundle && node scripts/prune-bundle.mjs",
    "dist": "pnpm -w run build && pnpm bundle:server && electron-builder",
    "dist:dir": "pnpm -w run build && pnpm bundle:server && electron-builder --dir"
  },
  "devDependencies": {
    "@types/node": "^20",
    "electron": "^43.3.0",
    "electron-builder": "^25.1.8",
    "tsup": "^8.4.0",
    "typescript": "^5"
  }
}
